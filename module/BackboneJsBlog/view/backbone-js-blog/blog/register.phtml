<?php
$form = $this->form;
$form->setAttribute('action', $this->url('backbone-js-blog-register-login', array('action' => 'register')));
$form->prepare();
?>

<h4>Register</h4>
<div class="row">
    <?php
    echo $this->form()->openTag($form);
    echo $this->formCollection($form);
    echo $this->form()->closeTag();
    ?>
</div>